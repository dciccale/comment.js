## Example with live demos

See `c_greet.json`, all scripts inside the `scritps` array will be loaded into the generated doc.

Run the following command inside this directory

```bash
$ commentjs c_greet.json
```
